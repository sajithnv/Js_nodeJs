// access modules and packages from 'npmjs.com'
// to access that library for first time, we should enter a comment in terminal "npm init"
// to install a library 'npm install upper-case' it will add one by one in package.json

// using upper-case

var uc=require('upper-case')
var ucData=uc.upperCase('sajith')
console.log(ucData)